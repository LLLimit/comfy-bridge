# Demo image workflow

This pack is explicitly a mock provider. It creates a deterministic abstract
PNG from the prompt without calling ComfyUI. Its only purpose is to make the
first mobile/Agent vertical slice runnable before a real API-format workflow is
imported.

