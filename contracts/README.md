# Contracts

`agent-api.openapi.yaml` describes the public snapshot API. WebSocket event
envelopes and private workflow configuration are described by their JSON
Schemas.

The schemas are deliberately small in the first vertical slice and will be
versioned instead of changed incompatibly.

