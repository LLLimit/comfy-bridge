# 安全政策

## 支持范围

项目仍处于早期阶段，目前仅维护默认分支上的最新代码。旧版本可能不会收到安全修复。

## 报告漏洞

请不要通过公开 Issue 报告可被利用的漏洞、泄露的凭据或绕过认证的方法。请优先使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告功能，并提供受影响的版本、重现步骤、潜在影响和可行的修复建议。

如果仓库尚未启用私密漏洞报告，请先通过不包含漏洞细节的普通 Issue 请求维护者提供私密联系方式。

## 部署提醒

- 将设备 Token 视为密码，不要提交到 Git 或粘贴到公开日志。
- 仅在可信局域网中使用明文 HTTP；跨公网使用 HTTPS/WSS 或可信 VPN。
- 不要把 Bridge 端口直接暴露到公网。
- 正式发布 Android App 时使用自己的包名和签名密钥。
- 从第三方导入工作流前，检查其自定义节点与模型来源。
